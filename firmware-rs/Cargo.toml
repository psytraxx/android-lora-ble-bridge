[package]
edition = "2024"
name = "firmware-rs"
version = "0.1.0"

[[bin]]
name = "firmware-rs"
path = "./src/bin/main.rs"

[dependencies]
esp-hal = { version = "1.0.0", features = ["log-04", "unstable", "esp32s3"] }

esp-rtos = { version = "0.2.0", features = [
  "embassy",
  "esp-alloc",
  "esp-radio",
  "log-04",
  "esp32s3",
] }

esp-bootloader-esp-idf = { version = "0.4.0", features = ["log-04", "esp32s3"] }
log = "0.4.29"

bt-hci = "0.6.0"
embassy-executor = { version = "0.9.1", features = ["log"] }
embassy-time = { version = "0.5.0", features = ["log"] }
embedded-hal = { version = "1.0", features = ["defmt-03"] }
embedded-hal-async = { version = "1.0", features = ["defmt-03"] }
embedded-io = "0.7.1"
embedded-io-async = "0.7.0"
esp-alloc = { version = "0.9.0" }
esp-backtrace = { version = "0.18.1", features = [
  "panic-handler",
  "println",
  "esp32s3",
] }
esp-println = { version = "0.16.1", features = ["log-04", "esp32s3"] }
esp-radio = { version = "0.17.0", default-features = false, features = [
  "ble",
  "esp-alloc",
  "log-04",
  "unstable",
  "esp32s3",
] }
trouble-host = { version = "0.5.1", features = [
  "peripheral",
  "gatt",
  "derive",
  "default-packet-pool-mtu-512",
], default-features = false }
critical-section = "1.2.0"
static_cell = "2.1.1"
embassy-sync = { version = "0.7.2", features = ["log"] }
heapless = "0.9.2"
embassy-futures = { version = "0.1.2", features = ["log"] }
lora-phy = "3.0.1"
embassy-embedded-hal = "0.5.0"
esp-storage = { version = "0.8.1", features = ["esp32s3"] }
embedded-storage = "0.3"
nb = "1"

[profile.dev]
# Rust debug is too slow.
# For debug builds always builds with some optimization
opt-level = "s"

[profile.release]
codegen-units = 1        # LLVM can perform better optimizations using a single thread
debug = 2
debug-assertions = false
incremental = false
lto = 'fat'
opt-level = 's'
overflow-checks = false
